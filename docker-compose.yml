version: "3.8"
services:
  postgres:
    image: postgres:15
    container_name: notifyhub_postgres
    environment:
      POSTGRES_USER: notifyhub
      POSTGRES_PASSWORD: notifyhub123
      POSTGRES_DB: notifyhub_db
    ports:
      - "5432:5432"
    volumes:
      - notifyhub_pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: notifyhub_redis
    ports:
      - "6379:6379"

volumes:
  notifyhub_pgdata:
